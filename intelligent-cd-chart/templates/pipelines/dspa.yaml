{{- if .Values.pipelines.enabled }}
---
apiVersion: datasciencepipelinesapplications.opendatahub.io/v1
kind: DataSciencePipelinesApplication
metadata:
  name: dspa
  namespace: {{ .Values.global.namespace }}{{ .Values.pipelines.namespaceSuffix }}
  annotations:
    argocd.argoproj.io/sync-wave: "10"
spec:
  apiServer:
    artifactSignedURLExpirySeconds: 60
    caBundleFileMountPath: ""
    caBundleFileName: ""
    cacheEnabled: true
    deploy: true
    enableOauth: true
    enableSamplePipeline: false
    managedPipelines:
      instructLab:
        state: Managed
    pipelineStore: database
  database:
    disableHealthCheck: false
    mariaDB:
      deploy: true
      pipelineDBName: mlpipeline
      pvcSize: 10Gi
      username: mlpipeline
  dspVersion: v2
  objectStorage:
    disableHealthCheck: false
    enableExternalRoute: false
    externalStorage:
      basePath: ""
      bucket: {{ .Values.global.namespace }}{{ .Values.pipelines.namespaceSuffix }}-{{ .Values.pipelines.connection.secretName }}
      host: "{{ .Values.pipelines.connection.minioEndpoint }}"
      port: "{{ .Values.pipelines.connection.minioPort }}"
      region: "{{ .Values.pipelines.connection.minioRegion }}"
      s3CredentialsSecret:
        accessKey: AWS_ACCESS_KEY_ID
        secretKey: AWS_SECRET_ACCESS_KEY
        secretName: "{{ .Values.pipelines.connection.secretName }}"
      scheme: "{{ .Values.pipelines.connection.scheme }}"
  persistenceAgent:
    deploy: true
    numWorkers: 2
  podToPodTLS: true
  scheduledWorkflow:
    cronScheduleTimezone: UTC
    deploy: true
{{- end }}